input {
  udp { port => 5000 codec => json }
}

filter {

    json {
      source => "message"
    }

}

output {
  stdout {
    codec => rubydebug
  }

  elasticsearch {
    host => "127.0.0.1"
    port => "9200"
    protocol => "http"
  }
}
