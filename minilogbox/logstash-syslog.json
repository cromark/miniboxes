input {
  udp { port => 5000 type => "logspout" }
}

filter {

  if [type] == "logspout" {
    json {
      source => "message"
    }
  }

}

output {
  stdout {
    codec => rubydebug
  }

  elasticsearch {
    host => "127.0.0.1"
    port => "9200"
    protocol => "http"
  }
}
